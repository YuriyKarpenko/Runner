<UserControl x:Class="Runner.Views.OptionsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="450"
             >
  <StackPanel Orientation="Vertical">
    <StackPanel Orientation="Horizontal" Margin="3">
      <Button Command="{Binding CmdAdd}" Content="{StaticResource img_Plus}" Style="{StaticResource cmd_h}"/>
      <Button Command="{Binding CmdDel}" Content="{StaticResource img_Delete}" Style="{StaticResource cmd_h}"/>
      <Button Command="{Binding CmdSave}" Content="{StaticResource img_Save}" Style="{StaticResource cmd_h}"/>
    </StackPanel>
    <ListBox SelectedItem="{Binding Selected}"
             ItemsSource="{Binding OptionItems}"
             HorizontalContentAlignment="Stretch"
                 >
    </ListBox>
  </StackPanel>
</UserControl>
